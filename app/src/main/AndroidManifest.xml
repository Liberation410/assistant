<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zjgsu.shenyilin.assistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".function_choose" />
        <activity android:name=".management_user" />
        <activity android:name=".management_note" />
        <activity android:name=".management_entertainment" />
        <activity android:name=".management_relationship" />
        <activity android:name=".management_life" />
        <activity android:name=".management_srudy" />
        <activity android:name=".managemet_study"></activity>
    </application>

</manifest>